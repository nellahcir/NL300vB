<h1>New checkpoint</h1>

<%= form_for(@checkpoint) do |f| %>
  <%= f.error_messages %>
    <%= render :partial => 'form' %>
  <p>
    <%= f.submit 'Create' %>
  </p>
<% end %>

<%= link_to image_tag("/images/up.png"), checkpoints_path %>