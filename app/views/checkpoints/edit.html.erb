<h1>Editing checkpoint</h1>

<%= form_for(@checkpoint) do |f| %>
  <%= f.error_messages %>
    <%= render :partial => 'form' %>
  <p>
    <%= f.submit 'Update' %>
  </p>
<% end %>

<%= link_to image_tag("/images/up.png"), checkpoints_path %>