<h1>Editing status</h1>

<%= form_for(@status) do |f| %>
  <%= f.error_messages %>
    <%= render :partial => 'form' %>
  <p>
    <%= f.submit 'Update' %>
  </p>
<% end %>

<%= link_to image_tag("/images/up.png"), statuses_path %>