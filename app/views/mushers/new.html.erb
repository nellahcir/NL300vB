<h1>New musher</h1>

<%= form_for(@musher) do |f| %>
  <%= f.error_messages %>
    <%= render :partial => 'form' %>
  <p>
    <%= f.submit 'Create' %>
  </p>
<% end %>

<%= link_to image_tag("/images/up.png"), mushers_path %>