<h1>Editing checkout</h1>

<%= form_for(@checkout) do |f| %>
  <%= f.error_messages %>
    <%= render :partial => 'form' %>
  <p>
    <%= f.submit 'Update' %>
  </p>
<% end %>

<%= link_to image_tag("/images/up.png"), checkouts_path %>